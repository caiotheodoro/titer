docs/private/
.cache/
cache/
*.cache.json
.env
.venv/
__pycache__/
*.pyc
.pytest_cache/
results/scratch/
.DS_Store
data/

# Derived corpus artifacts. results/corpus_public.jsonl is ~1.5 GB - it was
# committed by accident and made the repo unpushable (GitHub caps files at
# 100 MB). The corpus is published as an HF dataset (D008), never through git.
results/*.jsonl
data/scholar.jsonl
